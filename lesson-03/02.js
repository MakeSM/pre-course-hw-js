const words = ['яблоко', 'банан', 'апельсин', 'манго', 'киви'];

const element = 'апельсин';

if (words.indexOf(element) !== -1) {
   console.log('Ура! нашел');
} else {
   console.log('Придется поискать в другом магазине…');
}
